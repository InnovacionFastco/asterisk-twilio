; Renderizado automáticamente por bootstrap-asterisk.sh
; Ajusta las variables de entorno HTTP_BIND_ADDRESS, HTTP_PORT y HTTP_PREFIX para modificar estos valores.

[general]
enabled = yes
bindaddr = __HTTP_BIND_ADDRESS__
bindport = __HTTP_PORT__
prefix = __HTTP_PREFIX__
; Sirve el estado general y métricas útiles por defecto
;enable_static = no
;enable_status = yes
